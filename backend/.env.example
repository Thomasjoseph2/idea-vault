# Node server port (default 5000 if not specified)
PORT=5000

# Your MongoDB Atlas Connection String
# Replace this with your actual MongoDB URI before running
MONGODB_URI=YOUR_MONGODB_CONNECTION_STRING_HERE
